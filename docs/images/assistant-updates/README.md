# Assistant update screenshots

Captured on 24 September 2026 from real ReShiki windows on macOS using Computer Use and native window capture. These are unedited desktop screenshots, not generated mockups or headless figure renders.

The optimized binary was built from `519a04e` (assistant fixes plus the latest main at `e9256ee`). Its version remains 0.7.0 before the 0.7.1 version bump. A separate documentation app/profile was used for control demonstrations, preserving the user's open drafts.

| File                     | Demonstration                                                                                                                      |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| `source-and-result.png`  | Existing user conversation with the reference image and an applied editable reconstruction; captured without changing the drawing. |
| `codex-connected.png`    | Assistant opened in the documentation app and connected using the existing local sign-in; no generation request sent.              |
| `cp-star-projection.png` | Prepared planar/tilted Cp* comparison opened in the app; combined formula and atom count visible.                                  |
| `tilt-tool.png`          | Comparison ungrouped, only the right ligand selected, and X +15° applied with the toolbar.                                         |
| `charge-labels.png`      | Show charge labels cleared through Properties → Labels & chemistry → Atom labels & numbering.                                      |
| `aromatic-wedge.png`     | Repaired dimer opened, ungrouped for inspection, and a ring wedge selected; both aromatic ellipses retained.                       |
| `ring-curves.png`        | Complete, partial, and projected partial ring-curve examples opened in the app.                                                    |

The reference visible in `source-and-result.png` is the public-domain chemical drawing [Cp*2Rh2Cl4 2023.svg](https://commons.wikimedia.org/wiki/File:Cp*2Rh2Cl4_2023.svg) by Smokefoot. The pictures demonstrate rendering and editing; they are not independent validation of the complex's chemistry.
