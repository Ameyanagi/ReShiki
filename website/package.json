{
  "name": "@reshiki/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "bun scripts/sync-docs.mjs",
    "dev": "bun run sync && bun --bun astro dev",
    "build": "bun run sync && bun --bun astro build && bun scripts/check-links.mjs",
    "check": "bun run sync && bun --bun astro check"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "7.3.1",
    "@astrojs/starlight": "0.42.2",
    "astro": "7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
