[project]
name = "reshiki-engine"
version = "0.1.0"
description = "Local chemistry worker for ReShiki"
requires-python = ">=3.11,<3.14"
dependencies = ["rdkit>=2025.9,<2027"]

[dependency-groups]
dev = ["ruff>=0.16.8", "ty>=0.0.82", "pillow>=12.3,<13"]

[tool.uv]
package = false
required-version = ">=0.12.3"
# RDKit's optional bitmap drawing helpers use Pillow; ReShiki uses Rust instead.
# Scope this to the tested release. Pillow remains in dev for the image oracle.
exclude-dependencies = [
  { package = { name = "rdkit", version = "2026.3.6" }, dependencies = [
    "pillow",
  ] },
]
required-environments = [
  "sys_platform == 'darwin' and platform_machine == 'arm64'",
  # Windows ARM runs the same x64 chemistry worker through Windows 11 emulation.
  "sys_platform == 'win32' and platform_machine == 'AMD64'",
  "sys_platform == 'linux' and platform_machine == 'x86_64'",
  "sys_platform == 'linux' and platform_machine == 'aarch64'",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
# Worker/test entry points establish their local module search path first.
"engine/worker.py" = ["E402"]
"tests/*.py" = ["E402"]
"scripts/regenerate_templates.py" = ["E402"]

[tool.ty.environment]
python-version = "3.12"
extra-paths = ["scripts"]

[tool.ty.src]
include = ["engine", "scripts"]
